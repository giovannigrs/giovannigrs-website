/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains :['grsouza.000webhostapp.com','avatars.githubusercontent.com'],
  }
}

module.exports = nextConfig
